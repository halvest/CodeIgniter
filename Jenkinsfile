pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Tambahkan perintah build di sini
            }
        }
        
        stage('Test') {
            steps {
                echo 'Testing...'
                // Tambahkan perintah test di sini
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Tambahkan perintah deploy di sini
            }
        }
    }
}
